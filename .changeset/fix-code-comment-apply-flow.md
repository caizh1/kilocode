---
"chipmate": patch
---

让单个函数注释生成后直接进入 Diff，在源码预览标签被替换时自动重新打开目标文件，并从完整函数候选中确定性提取纯注释插入，避免自定义声明前缀导致随机定位失败；生成或校验失败后立即释放任务状态，不再因未处理失败通知而误报已有任务仍在运行。复杂函数现在会按控制区域生成更多关键行间注释，修订既有函数说明时会保留 Doxygen 标签、顺序、参数方向和参数名。
