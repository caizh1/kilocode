---
"chipmate": minor
"@chipmate/cli": minor
"@chipmate/sdk": minor
---

在普通 Agent 的侧边栏、编辑器聊天和 Agent Manager 中按轮审阅文件修改，支持整轮、单文件、文本差异块撤销及恢复。停止后保留实际修改，等待写入收尾再开放撤销；历史轮次撤销保留后续不冲突的编辑，并在冲突、记录不完整、未保存编辑或仍有 Agent 写入时拒绝操作。

将执行中的修改状态收起为输入框上方的小三角，按需展开记录栏；收尾后在所属回答底部显示紧凑审阅入口，释放小屏聊天空间并保留历史阅读位置。
