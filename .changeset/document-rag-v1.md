---
"@chipmate/chipmate-indexing": patch
"@chipmate/cli": patch
"@chipmate/sdk": patch
"chipmate": patch
---

Add Document RAG indexing and the `document_search` tool for workspace documents, with bundled `pdftotext` support for internal offline Windows VSIX builds.
