---
"chipmate": patch
---

修复取消 Patent Radar 扫描或 Reload Window 后再次启动仍显示旧状态、进度轮询可能被慢请求锁死的问题；扫描启动和恢复会先持久化 Run 并立即返回，由服务端后台继续执行，状态读取或启动失败会显示真实错误。取消或失败的进度弹窗可重新开始；模块扫描会沿用专利中心已选择的分析模型，并按 SDK 契约提交完整范围预览请求体。同一工作区的重复启动现在只创建一个任务；已有任务的范围或模型不一致时会明确阻止新请求，扫描按钮和取消按钮也会等待当前回执并忽略过期响应。
