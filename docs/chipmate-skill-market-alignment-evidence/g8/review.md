# G8 Review

Date: 2026-07-12

Status: COMPLETE

## Delivered

- Implemented the complete 14-event dictionary and authenticated `/api/v1/events/batch` ingestion with batches capped at 100.
- Server derives the stable pseudonymous user, hashes client instances, bounds event age and context, and rejects sensitive or unknown fields.
- Web and Kilo batch asynchronously; credentials are read only at flush time and never placed in event payloads.
- Worker aggregates daily metrics, retains raw events for 90 days and daily aggregates for 365 days, and performs cleanup after aggregation.
- Added global analytics, owner-only Skill funnels, Web `/me` and `/analytics`, and Kilo aligned analytics updates.
- Added `analytics.updated`, scoped Web/Kilo refresh, automatic SSE backoff, `Last-Event-ID`, ETag/catalog-version compensation, and catalog query coalescing.
- Added adversarial archive, XSS, credential, ownership, CSRF, deep-link, retention, reconnect, 10,000-Skill, 200-concurrent-user, and real-render isolation coverage.

## Performance gate

Source: `load.json` generated by `node --import tsx scripts/g8-load.ts`.

- Dataset: 10,000 Skills.
- Load: 200 concurrent HTTP users / 200 requests.
- Market errors: 0; p95 40.12 ms against the locally declared 2,000 ms G8 budget.
- Real Word/Mermaid errors: 0.
- Empty-load render p95: 2,041.39 ms.
- Loaded render p95: 2,061.59 ms.
- Render degradation: 0.99%, below the 20% gate.

## Verification

- Server typecheck, lint, tests, contract drift check, and build: PASS.
- API: 14/14 PASS; Web: 3/3 PASS; contracts: 4/4 PASS; DB: 3/3 PASS; Skill spec: 6/6 PASS.
- Kilo extension typecheck and lint: PASS.
- Kilo focused Marketplace suite before the reconnect addition: 50/50 PASS; reconnect/security focused suite after the addition: 13/13 PASS.
- Web production build: PASS; first route 80.87 KiB gzip, below 300 KiB.

## Known limitations carried to G9

- Microsoft Edge is `USER_WAIVED / NOT_RUN` by explicit user instruction.
- Full Kilo `bun run test:unit` and `bun run knip` retain unrelated pre-existing failures; focused Marketplace tests pass.
- Docker/Linux, offline bundle, fresh VSIX packaging, real profile installation, and final rollback/migration exercise remain G9 work.
