---
"chipmate": patch
"@chipmate/cli": patch
"@chipmate/chipmate-indexing": patch
---

Let ChipMate v2 run alongside ChipMate in one VS Code profile with isolated commands, settings, storage, backend state, indexing data, and autocomplete ownership. Existing users must install this identity manually once; later v2 builds support verified in-app updates up to 256 MiB.
