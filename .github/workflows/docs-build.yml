name: docs-build

on:
  push:
    branches: [main]
    paths:
      - "packages/chipmate-docs/**"
  pull_request:
    paths:
      - "packages/chipmate-docs/**"
  workflow_dispatch:

jobs:
  build:
    name: Build docs site
    runs-on: blacksmith-4vcpu-ubuntu-2404 # chipmate_change
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6 # chipmate_change

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Build site
        run: bun run --filter @chipmate/chipmate-docs build
        env:
          NEXT_PUBLIC_POSTHOG_KEY: ${{ secrets.POSTHOG_API_KEY }}

      - name: Run tests
        run: bun run --filter @chipmate/chipmate-docs test
