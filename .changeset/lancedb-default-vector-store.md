---
"chipmate": patch
"@chipmate/chipmate-indexing": patch
"@chipmate/cli": patch
---

Use LanceDB as the default codebase indexing vector store so new indexing setups do not require a separate Qdrant server.
