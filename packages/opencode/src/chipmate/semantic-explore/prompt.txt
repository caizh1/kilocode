这是一个候选定位任务，不要修改代码。

待探索的用户功能意图：$ARGUMENTS

1. 将用户的功能意图转换成一个简洁的英文查询。
2. 调用一次 `semantic_search`。
3. 将结果按模块和实现路径归类，不要默认排名第一就是正确答案。
4. 最多保留 3 个最可能的候选文件或符号。
5. 使用 `grep` 验证准确标识符、调用点和引用关系。
6. 使用 `read` 阅读必要范围，确认真实控制流。
7. 定稿前必须做一次反证检查：
   - 若结果命中测试、反例、允许/拒绝路径或错误恢复，至少使用 `read` 阅读 1 处最相关证据；
   - 对每条因果链确认“实际保证什么、不保证什么”；
   - 不得把结构校验、候选隔离、回读或回滚写成特定异常检测，除非源码或测试直接证明；
   - 反证成立时修正结论，并保留真实保护边界。
8. 若语义结果低相关、分散、只是重复样板、工具不可用或索引未就绪，明确说明它没有贡献，并退化为 `glob`、`grep`、`read`。
9. 输出：
   - 最可能的实现入口；
   - 关键文件和符号；
   - 证据链；
   - 关键反证和保护边界；
   - 语义检索是否真正提供了额外价值。
