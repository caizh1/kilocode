---
"@kilocode/kilo-indexing": patch
"@kilocode/cli": patch
"chipmate": patch
---

Start internal code and document indexes automatically in Code Graph, Code RAG, then Document RAG order while preserving explicit indexing opt-outs.
