# OpenSSD 五函数分级回归

## 本轮范围

- 日期：2026-08-01
- 代码库：`/Users/archer/Work/cosmos-plus-openssd`
- 模型：`chipmate/deepseek-v4-flash`
- 路径：每个函数使用一个全新 Code 会话生成，再用另一个全新 Code 会话独立复核
- 样本：5 个此前未用于本功能回归的真实 C 函数，按 4、10、30、63、88 行递增
- 原始数据：`/tmp/chipmate-code-comments-openssd-round2-v1`
- OpenSSD 源码未写入注释，只读取源码和既有索引；最终候选仅保存在 QA 结果中

## 运行结果

|复杂度|函数|生成耗时|复核耗时|最终状态|复核动作|
|---|---|---:|---:|---|---|
|极简|`AllocateTempDataBuf`|18.287 秒|37.253 秒|可生成 Diff|修订 1 处事实定性|
|简单|`UpdateDataBufEntryInfoBlockingReq`|15.833 秒|40.206 秒|可生成 Diff|通过|
|中等|`AllocateDataBuf`|23.892 秒|28.313 秒|可生成 Diff|通过|
|复杂|`CheckReqStatus`|34.873 秒|37.537 秒|可生成 Diff|通过|
|高复杂|`ExecuteNandReq`|47.584 秒|25.250 秒|可生成 Diff|通过|

- 10/10 个 Code 会话正常完成，0 超时，0 结构化 JSON 错误，0 解析失败。
- 单会话平均 30.903 秒，最长 47.584 秒；每函数两轮合计平均 61.806 秒。
- 5/5 个样本通过确定性注释插入、锚点和非注释 token 安全校验。
- 总模型调用耗时之和为 309.028 秒；这是 5 个函数顺序批量回归的总和，不是产品内单函数操作耗时。

## 评分

评分固定采用：事实与证据 40 分；契约、状态和副作用覆盖 20 分；克制性与避免重复 20 分；位置、中文表达和项目风格 10 分；写入安全性 10 分。

|函数|事实与证据|契约/状态/副作用|克制性|位置/中文/风格|写入安全|总分|
|---|---:|---:|---:|---:|---:|---:|
|`AllocateTempDataBuf`|40|18|12|9|10|89|
|`UpdateDataBufEntryInfoBlockingReq`|40|19|13|9|10|91|
|`AllocateDataBuf`|40|20|14|9|10|93|
|`CheckReqStatus`|40|20|18|9|10|97|
|`ExecuteNandReq`|40|20|19|9|10|98|
|平均|40.0|19.4|15.2|9.0|10.0|93.6|

## 逐函数判断

### `AllocateTempDataBuf`：89 分

最终注释正确指出临时缓冲条目数等于 `USER_DIES`，因此 die 号可直接作为条目标识。首轮错误地称其为“简化占位实现”，复核会话根据 `data_buffer.h` 的容量定义和调用方证据完成修订。最终事实正确，但对四行函数使用六行说明偏长，主要扣在克制性。

### `UpdateDataBufEntryInfoBlockingReq`：91 分

正确说明请求通过 `prevBlockingReq/nextBlockingReq` 追加到缓冲项阻塞链尾，且 `CheckBufDep` 和释放路径确实依赖该链。函数说明有价值；两条行内注释基本逐句解释赋值，存在轻度重复，因此克制性未满分。

### `AllocateDataBuf`：93 分

正确覆盖 LRU 尾条目选择、移到链头、旧 LSA 哈希映射摘除以及供调用方复用的副作用。函数说明完整，哈希失效注释有明确价值；另外两条链表行内注释较接近代码表面行为。

### `CheckReqStatus`：97 分

正确识别 `COMPLETION_FLAG`、`CHECK`、`REPORT`、`NONE` 四种检查阶段，以及 `CHECK -> REPORT -> CHECK` 的跨调用轮询。复核还核实 `NONE` 只由 RESET 和 SET_FEATURE 路径设置。注释集中解释异步时序和状态流转，重复较少。

### `ExecuteNandReq`：98 分

正确覆盖 IDLE/EXE 状态、读 Trigger/Transfer 两阶段、失败重试不出队、Transfer 失败回退 Trigger，以及终态出队契约。三条行内注释都解释非显然时序或重试原因，没有发现严重或轻微事实错误。

## 质量结论

- 本轮最终候选事实正确率：100%，严重幻觉：0。
- 5/5 可产生安全 Diff，样本完成率：100%。
- 平均 93.6 分，超过单轮 85 分门槛。
- 双会话复核不是形式步骤：它实际拦截并修正了 1 个首轮错误定性；其余 4 个样本独立核实后通过。
- 当前主要问题已经从超时和 JSON 解析失败，转为简单函数的注释克制性。该问题不影响写入安全，但会增加阅读噪音。
- 这只能证明当前 5 个新 OpenSSD 样本通过，不能替代原计划的 50 函数正式验收，因此产品整体状态仍应标记为“未完成正式验证”。
