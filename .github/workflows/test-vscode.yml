name: test-vscode

on:
  push:
    branches:
      - main
      - dev
    paths:
      - "packages/chipmate-vscode/**"
      - "packages/ui/**"
      - "packages/chipmate-ui/**"
      - ".github/workflows/test-vscode.yml"
  pull_request:
    paths:
      - "packages/chipmate-vscode/**"
      - "packages/ui/**"
      - "packages/chipmate-ui/**"
      - ".github/workflows/test-vscode.yml"
  workflow_dispatch:

jobs:
  unit:
    name: unit tests
    runs-on: blacksmith-4vcpu-ubuntu-2404 # chipmate_change
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6 # chipmate_change
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Run unit tests
        working-directory: packages/chipmate-vscode
        run: bun run test:unit

      - name: Check lint (ESLint)
        working-directory: packages/chipmate-vscode
        run: bun run lint

      - name: Check formatting (prettier)
        working-directory: packages/chipmate-vscode
        run: bun run format:check

      - name: Check for dead code (knip)
        working-directory: packages/chipmate-vscode
        run: bun run knip

      - name: Check for chipmate_change markers
        working-directory: packages/chipmate-vscode
        run: bun run check-chipmate-change
