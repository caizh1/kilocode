name: beta

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"

jobs:
  sync:
    if: github.repository == 'ChipMate-Org/chipmate'
    runs-on: blacksmith-4vcpu-ubuntu-2404
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6 # chipmate_change
        with:
          persist-credentials: false # chipmate_change
          fetch-depth: 0

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Setup Git Committer
        id: committer
        uses: ./.github/actions/setup-git-committer
        with:
          # chipmate_change start
          chipmate-maintainer-app-id: ${{ secrets.CHIPMATE_MAINTAINER_APP_ID }}
          chipmate-maintainer-app-secret: ${{ secrets.CHIPMATE_MAINTAINER_APP_SECRET }}
          # chipmate_change end

      - name: Sync beta branch
        env:
          GH_TOKEN: ${{ steps.committer.outputs.token }}
        run: bun script/beta.ts
