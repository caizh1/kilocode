---
"chipmate": patch
"@kilocode/cli": patch
"@kilocode/kilo-indexing": patch
---

Let ChipMate v2 run alongside Kilo Code in one VS Code profile with isolated commands, settings, storage, backend state, indexing data, and autocomplete ownership. Existing users must install this identity manually once; later v2 builds support verified in-app updates up to 256 MiB.
